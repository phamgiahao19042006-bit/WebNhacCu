@model WebNhacCu.Areas.Admin.Models.DashboardViewModel

@{
    ViewData["Title"] = "Trang chủ Admin";
    Layout = "~/Areas/Admin/Views/Shared/_admin.cshtml";
}
<<<<<<< HEAD

<div class="container-fluid p-4">
    <h3 class="fw-bold mb-4">Tổng Quan Hệ Thống</h3>

    <!-- 4 THỐNG KÊ TỰ ĐỘNG -->
    <div class="row g-3 mb-4">
        <!-- Doanh Thu Tháng -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm p-3 bg-white">
                <div class="text-muted small">Doanh Thu Tháng</div>
                <div class="fs-4 fw-bold text-success">@Model.DoanhThuThang.ToString("N0") đ</div>
                <div class="mt-2"><i class="fa-solid fa-sack-dollar text-warning fs-5"></i></div>
            </div>
        </div>

        <!-- Hóa Đơn Mới -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm p-3 bg-white">
                <div class="text-muted small">Hóa Đơn Mới</div>
                <div class="fs-4 fw-bold text-dark">@Model.HoaDonMoi</div>
                <div class="mt-2"><i class="fa-solid fa-receipt text-primary fs-5"></i></div>
            </div>
        </div>

        <!-- Sản Phẩm Tồn Kho -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm p-3 bg-white">
                <div class="text-muted small">Sản Phẩm Tồn Kho</div>
                <div class="fs-4 fw-bold text-dark">@Model.SanPhamTonKho</div>
                <div class="mt-2"><i class="fa-solid fa-boxes-stacked text-danger fs-5"></i></div>
            </div>
        </div>

        <!-- Khách Hàng Mới -->
        <div class="col-md-3">
            <div class="card border-0 shadow-sm p-3 bg-white">
                <div class="text-muted small">Khách Hàng Mới</div>
                <div class="fs-4 fw-bold text-dark">@Model.KhachHangMoi</div>
                <div class="mt-2"><i class="fa-solid fa-user-plus text-info fs-5"></i></div>
            </div>
        </div>
    </div>
</div>

=======
>>>>>>> 0b948152837b785c2ae07eac8381a7882d2c1566
