@model WebNhacCu.Models.ViewModels.ProductViewModel

@{
    ViewData["Title"] = "Sản phẩm";
}

<h2>Danh sách sản phẩm</h2>

<div class="product-grid">

    @foreach (var product in Model.Products)
    {
        <partial name="_ProductCard" model="product" />
    }

</div>